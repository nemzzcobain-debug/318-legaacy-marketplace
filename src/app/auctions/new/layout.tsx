import { privatePageMetadata } from '@/lib/seo'

export const metadata = privatePageMetadata('Ajouter une enchère')

export default function NewAuctionLayout({ children }: { children: React.ReactNode }) {
  return children
}
