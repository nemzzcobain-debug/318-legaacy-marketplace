import { privatePageMetadata } from '@/lib/seo'

export const metadata = privatePageMetadata('Mes achats')

export default function PurchasesLayout({ children }: { children: React.ReactNode }) {
  return children
}
