import { privatePageMetadata } from '@/lib/seo'

export const metadata = privatePageMetadata('Ma liste de suivi')

export default function WatchlistLayout({ children }: { children: React.ReactNode }) {
  return children
}
