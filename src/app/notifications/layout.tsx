import { privatePageMetadata } from '@/lib/seo'

export const metadata = privatePageMetadata('Mes notifications')

export default function NotificationsLayout({ children }: { children: React.ReactNode }) {
  return children
}
