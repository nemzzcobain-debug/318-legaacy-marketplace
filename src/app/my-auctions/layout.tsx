import { privatePageMetadata } from '@/lib/seo'

export const metadata = privatePageMetadata('Mes enchères')

export default function MyAuctionsLayout({ children }: { children: React.ReactNode }) {
  return children
}
