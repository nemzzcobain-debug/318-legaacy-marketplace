import { privatePageMetadata } from '@/lib/seo'

export const metadata = privatePageMetadata('Mon espace')

export default function DashboardLayout({ children }: { children: React.ReactNode }) {
  return children
}
