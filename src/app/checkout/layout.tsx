import { privatePageMetadata } from '@/lib/seo'

export const metadata = privatePageMetadata('Finaliser mon achat')

export default function CheckoutLayout({ children }: { children: React.ReactNode }) {
  return children
}
